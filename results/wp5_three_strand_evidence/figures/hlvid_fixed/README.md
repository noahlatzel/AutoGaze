# HLVid fixed-budget publication assets

These PDF/SVG/300-dpi PNG figures render the existing R2e compact tables using
exactly base seeds 440826, 440827, and 440828 across K16, K24, and K32.

The main figure retains primary question-micro QA and the published 90% paired
video-cluster intervals. The second figure is a secondary aggregation sensitivity
with point estimates only. Extra K16 endpoints and all pending causal controls
are excluded.

Full captions are in `figure_captions.md`. Each figure has JSON provenance,
renderer/input/output hashes, and exact comparison data. `data_checks.json`
records source-table validation. Safe repository-root commands and plotting
dependency details are in `../../FIGURE_REGENERATION.md` and `../../figures.csv`.

No inference or bootstrap was recomputed. These are new WP5 publication assets;
the original experimental bundle remains unchanged.
